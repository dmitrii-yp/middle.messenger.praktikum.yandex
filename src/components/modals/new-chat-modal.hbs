  <div
    class='modal {{#if active}}absolute{{else}}hidden{{/if}} top-[50%] left-[50%] -mt-[200px] -ml-[170px] h-[280px] w-[340px] rounded-lg bg-dark2 p-7 shadow-2xl'
  >
    {{{H2 header='create_chat'}}}
    <form action='#'>
      {{{InputBlock
        name='chat_title'
        label='Chat title'

      }}}
      <div
      class='relative -top-3 text-center font-monospace text-xs text-red0'
    >&nbsp;{{error}}&nbsp;</div>
      {{{SubmitButton label='Create' onClick=onCreateNewChatSubmit}}}
    </form>
    <div class='mt-2 text-center'>{{{TextButton label='Cancel' onClick=onModalCancelClick red=true}}}</div>

  </div>

