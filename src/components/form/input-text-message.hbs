<input
      class="my-0 h-8 w-full appearance-none rounded-md bg-dark3 text-left indent-2 font-monospace text-sm text-white focus:border focus:border-purple2 focus:outline-none invalid:border invalid:border-red0 focus:invalid:border-red0"
      type="box"
      placeholder="Type message here..."
      id="message"
      name="message"
      {{#if value}}
      value={{value}}
      {{/if}}
    />
